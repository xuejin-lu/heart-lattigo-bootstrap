# FIX-001-DIAG-POLY — Replay formal Chebyshev powers after FIX-001

## Purpose

FIX-001 changed Fast Chebyshev power generation to apply the correction term after Rescale and produced repaired Fast commit:

`87be78ff3c591932699aba63d3be46ca306a6eea`

However, repaired stage-level diagnosis still shows the first formal LogN13 divergence at Q3 `EvalMod(real)` with exactly the historical max component error:

`0.12023816407863477`

This task must determine whether the repaired code actually fixes the **formal E2 power-generation path** used by Bootstrap.

Answer exactly:

1. Does formal `T2` now pass on the real E2 input?
2. Do all generated formal Chebyshev powers now pass?
3. If all powers pass but the whole degree-30 polynomial still fails, does the first supported cause move to Paterson–Stockmeyer accumulation / scale alignment?

Do not repair anything in this task.

---

## Fixed provenance

Primary repository: `xuejin-lu/heart-lattigo-bootstrap`

Expected Primary remote base:

`ecf53606d971a0f90b7548e8b73a3dd5ff9e1886`

Repaired Fast backend:

`87be78ff3c591932699aba63d3be46ca306a6eea`

Historical Fast baseline for comparison only:

`ce79b861c9b4ecb45f7a42ca5de2e98dbbdb9ef2`

Standard reference remains:

`5dbffbdea05394de2ca3a432ed5318aa832e3f40`

Follow both repositories' `AGENTS.md`. Secondary must be clean before starting. Do not reset/stash/discard unrelated work.

The parent FIX-001 remains incomplete.

---

## Formal workload

Reproduce exactly the same formal E2 real input used by:

- `EXP-002-C-DIAG-EVALMOD`
- `EXP-002-C-DIAG-POLY`
- `EXP-002-C-DIAG-T2`

Use:

- LogN = 13
- all 4096 slots
- `configs/bootstrap_config.logN13.json`
- same deterministic application input
- same Q2 real branch after `CoeffsToSlots`
- same E1 scale reinterpretation
- same E2 scalar offset
- same Mod1 polynomial parameters
- same `ModUpThenEncode` order

Require the reproduced E2 vector to match the historical formal E2 vector within the existing `1e-2` threshold before any causal claim.

If not, stop with `DIAGNOSTIC_INPUT_MISMATCH`.

---

## Reuse the previous power diagnostic method

Reuse the validated machinery from:

`specs/EXP-002-C-DIAG-POLY-powers.md`

Do not invent a different power oracle.

Temporary, uncommitted, build-tagged Secondary diagnostic helpers are allowed only to snapshot the actual internal `fastPowerBasis` powers generated by the repaired production implementation.

Requirements:

- invoke the real repaired `workspace.generatePowers(...)` path;
- deep-copy generated powers only for diagnosis;
- do not reimplement power generation in the harness;
- remove all temporary Secondary diagnostic files before completion;
- Secondary must return clean at exact `87be78ff...`.

---

## Mandatory code-path provenance

Record evidence that the formal run is using the repaired source:

`circuits/ckks/polynomial/fast.go`

at commit `87be78ff...`.

Record:

- file blob/hash if practical;
- backend commit in raw artifacts;
- an explicit diagnostic marker showing that generated powers are obtained **after** the repaired post-Rescale correction ordering.

Do not modify production source merely to emit logging.

---

## Required power checkpoints

Enumerate every actual generated Chebyshev power in the formal degree-30 run.

For each generated `T_n`:

- record dependency split `(a,b,c)`;
- Degree;
- Level;
- exact Scale + log2;
- IsNTT / IsMontgomery;
- q0/q1 hashes for all components;
- q0/q1 projected decoded vector for all 4096 slots;
- plaintext expected `T_n(E2_actual)` for all 4096 slots;
- existing numerical metrics;
- pass/fail at fixed `1e-2` component threshold.

Use the same dependency-aware first-failure rule as the historical task.

---

## Explicit T2 before/after comparison

The historical formal baseline had:

- first dependency-supported failure: `T2`
- split `(1,1,0)`
- max component error ≈ `0.9995117188159196`

For repaired Fast, record:

- repaired T2 max component error;
- absolute improvement from historical;
- pass/fail;
- Level/Scale;
- whether T2 matches the post-Rescale-correction semantic oracle.

If repaired T2 still fails, classify:

`FIRST_SUPPORTED_CAUSE = repaired_power_generation_t2_still_fails`

and stop downstream causal interpretation after recording the remaining already-produced evidence.

---

## All-power classification

### Case A — T2 or another dependency-supported power still fails

`FIRST_SUPPORTED_CAUSE = repaired_power_generation`

Record first supported failing power and dependency graph.

This means FIX-001 did not fully repair the formal power path even if synthetic/focused regressions pass.

### Case B — every generated power passes

Run the actual repaired Fast whole polynomial evaluator on an independent formal E2 copy.

Require that it reproduces the current Q3 formal failure before attributing cause.

If:

- every generated power passes, and
- whole degree-30 polynomial still exceeds `1e-2`,

classify:

`FIRST_SUPPORTED_CAUSE = paterson_stockmeyer_accumulation_or_scale_alignment`

This is the main expected discriminating outcome of this task.

### Case C — every generated power passes and whole polynomial passes

Then compare the ordinary repaired public `EvalMod(real)` output.

If public EvalMod still fails while direct whole polynomial passes, classify:

`FIRST_SUPPORTED_CAUSE = post_polynomial_evalmod_logic`

and record the discrepancy.

If public EvalMod also passes, classify:

`FIRST_SUPPORTED_CAUSE = diagnostic_inconsistency`

because the repaired Q01 task reported Q3 failure at the same backend commit.

Do not guess further.

---

## Whole polynomial / Q3 cross-check

Record for repaired Fast:

1. direct formal degree-30 polynomial output vs source-backed plaintext polynomial oracle;
2. ordinary public `EvalMod(real)` vs Standard q01 Q3(real) reference;
3. direct polynomial output vs public EvalMod internal polynomial-stage output where available without production modification.

Use the same fixed `1e-2` threshold.

Record old baseline whole-polynomial error if available from historical artifacts and compare it to repaired value.

---

## PS plan evidence when all powers pass

If Case B applies, record the actual repaired Paterson–Stockmeyer plan metadata:

- base;
- number of baby-step blocks;
- each block degree;
- planned Level;
- planned Scale;
- giant-step merge structure;
- final planned vs actual Level/Scale.

Do not yet instrument every PS operation. That becomes the next task only if this task proves powers are all correct.

---

## Required artifacts

Write temporary evidence to `/tmp` first while Secondary diagnostic helpers exist.

After Secondary is restored clean, create:

- `results/FIX-001-DIAG-POLY-logN13-fast.json`
- `results/FIX-001-DIAG-POLY-logN13-summary.json`

Do not overwrite historical EXP-002-C or previous FIX-001 artifacts.

The summary must contain:

- exact repaired Fast commit;
- E2 reproduction result;
- actual generated power set;
- per-power metrics;
- repaired T2 before/after comparison;
- all failing powers, if any;
- whole-polynomial result;
- public Q3(real) cross-check;
- selected first supported cause;
- PS plan metadata if all powers pass;
- clean-state/test evidence.

---

## Validation

Before completion:

- Primary `go test ./...` passes;
- repaired Secondary `go test ./...` passes;
- temporary Secondary diagnostics removed;
- Secondary remains exact `87be78ff...`, clean;
- Primary result artifacts committed/pushed normally;
- no Lattigo production source changes;
- no LogN16;
- no performance benchmarks;
- no EXP-003.

---

## Non-goals

Do not:

- modify or revert FIX-001;
- implement a PS fix;
- change Mod1 parameters;
- lower scale;
- add q2+;
- loosen tolerance;
- run LogN16;
- benchmark performance;
- start EXP-003.

This task ends when we know whether repaired formal power generation is actually correct and, if so, whether the first supported cause has moved to Paterson–Stockmeyer accumulation / scale alignment.